spring:
  application:
    name: movies-api
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/movies_api
    username: admin
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
csv:
  path:
    best_pictures: static/best_pictures.csv
tmp:
  valid:
    api:
      key: 42
